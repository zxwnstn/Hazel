버텍스 어레이는 실제적으로 버텍스 버퍼나, 인덱스 버퍼를 참조하는 데이터를 지니고 있다.
버텍스버퍼와 인데스 버퍼는 버텍스 어레이 안에 있게 된다.
이것은 그 그데이터가 문자그대로 안에 잇음을 의미 하는 것이 아니다.
그것은 그 두버퍼의 생명주기를 관리하지 않는다.
단순히 포인터를 의미한다. 버텍스 버퍼의 위치, 인덱스버퍼의 포인터만 있는것이다.

버텍스 어레이는 다렉에는 없다.
